# 游戏剧本导出器使用说明

`wf_story_export.py` 从 CN CDN store **只读**解码剧情 `scenario.orderedmap`，输出可读 Markdown 与逐指令 JSONL。它不会修改 store、`sync_pending.json`、CDN diff 或发布状态。

## 快速使用

在仓库根目录 `D:\WF\startpoint-cn` 运行：

```powershell
# 列出全部剧情或单一类别
python mod-tools/wf_story_export.py --list
python mod-tools/wf_story_export.py --list character_story_quest

# 单话；basename 有歧义时会列出候选并拒绝猜测
python mod-tools/wf_story_export.py --quest white_tiger_001
python mod-tools/wf_story_export.py --quest story_quest/main_chapter_01/main_chapter_01_01

# 角色可用 code、id 或 CN 名；按 identity_character_id 自动包含版本变体
python mod-tools/wf_story_export.py --character 白
python mod-tools/wf_story_export.py --character white_tiger
python mod-tools/wf_story_export.py --character 10

# 类别或全量导出
python mod-tools/wf_story_export.py --category story_event_quest
python mod-tools/wf_story_export.py --all

# 只做全量门禁，不创建输出目录
python mod-tools/wf_story_export.py --validate
```

通用参数：

- `--format md|jsonl|both`：默认 `both`。
- `--out <目录>`：默认 `mod-tools/work/story_export/`。
- `--store <upload目录>`：覆盖默认 `弹国服/WorldFlipper/dummy/download/production/upload`；目录不存在时立即失败，不扫描其他 profile 兜底。

导出前会先完成所选剧情的全部解码、渲染、临时写入与旧文件备份，再用同目录原子替换提交；提交中途失败会还原已替换文件，若回滚本身也失败则保留唯一 `.backup` 恢复件并在错误中给出精确路径。`--out` 等于或位于 store 内、剧情目录含 `.`/`..`、或目标叶子是 symlink/junction 时会拒绝执行，避免任何路径把输出重定向回只读 store。

正确的离线单测命令：

```powershell
python -m unittest discover -s mod-tools/tests -p "test_story_export.py" -v
# 或仓库 Python 全量回归
npm run test:python
```

## 输出布局与格式

每话写到：

```text
<out>/<category>/<scenario-relative-dir>/
├── scenario.md
└── scenario.jsonl
```

角色剧情 Markdown 标题为：

```markdown
# 白 个人剧情 第1话 「疑惑」
```

有声与无声对白示例：

```markdown
**阿尔克**🔊：久等了。  
　今天吃西红柿炖鸡肉哦。

**旁白**：夜深了。
```

- `🔊` 只在 `text_voice_path` 不是 `(None)` 时出现。
- 引号 CSV 内的真实换行、CRLF/CR 和字面 `\n` 都归一为 LF；Markdown 续行使用两个尾随空格和一个全角缩进。
- 空说话人明确显示为“旁白”。未知说话人保留原代号；`--validate` 同时计数。
- `_known` 是 `story_character` 中的独立权威别名，不会折叠回本体。例如 `white_tiger_known` 显示“兽人”。
- Markdown 展开对白、BGM、movie section、角色入退场、表情与静态背景。其余已知指令仍完整存在 JSONL 中。
- 未知 opcode 在 Markdown 中保留为 `<!-- kind=N cols={...} -->`，JSONL 同时保留全部 raw 列。

JSONL 每个物理行都是一个完整 JSON 对象：

```json
{"line": 5, "kind": 0, "op": "Text", "args": {"text_character_id": "alk", "text_body": "这里就是外面的世界？", "text_voice_path": "character/alk/voice/words/uwaa_1", "text_voice_immediately": false, "text_balloon_position": 3}, "raw": ["0"], "speaker_code": "alk", "speaker_name": "阿尔克", "text": "这里就是外面的世界？", "voice": "character/alk/voice/words/uwaa_1"}
```

实际 `raw` 始终是完整 43 列；少于或多于 43 列会作为损坏行拒绝，上例为排版简写。`(None)` 在 typed `args`/`voice` 中为 JSON `null`，在 `raw` 中仍是原始字符串。

## 数据清单与关联规则

主清单来自 `mod-tools/WF_PATHLIST_recovered.txt`：

| category | 数量 |
|---|---:|
| `character_story_quest` | 1312 |
| `story_quest` | 197 |
| `story_event_quest` | 157 |
| `advent_event` | 39 |
| `system_quest` | 3 |
| 合计 | 1708 |

工具只导出这 1708 条，分类由 scenario 自身逻辑路径决定。quest 关联源及 0-based 列号：

| category | quest 表 | 题名 | scenario | 其他关联 |
|---|---|---:|---:|---|
| 角色 | `master/quest/character_quest.orderedmap` | 3 | 126 | character=49，episode=50；key=quest_id |
| 主线 | `master/quest/main_quest.orderedmap` | 1 | 124 | col0=quest_id；三层数字 key 排序 |
| 普通剧情活动 | `master/quest/event/story_event_single_quest.orderedmap` | 2 | 126 | col0=quest_id |
| 世界剧情活动 | `master/quest/event/world_story_event_quest.orderedmap` | 2 | 125 | col0=quest_id |
| 降临 | `master/quest/event/advent_event_quest.orderedmap` | 2 | 130 | col0=quest_id |
| 系统 | `master/tutorial/triggered_tutorial.orderedmap` | 22 | 26 | col0=trigger code |

角色 code/id/名字来自 `character` 与 `character_text`。版本归族使用客户端字段 `character` col27 `identity_character_id`，不会把同名但不同 identity、且无个人剧情的 NPC/assist 混进结果。

当前 quest 表另引用 4 个 store 实存、但 pathlist 未收录的日文 `bcollabo` 降临剧情。`--validate` 会报告它们为 `referenced_not_in_pathlist=4`，不会静默把验收总数从 1708 改成 1712。

## APK 反编译核实后的完整 opcode

权威源来自当前 APK `弹国服/wf_M358262.apk` 内的 `assets/worldflipper_android_release.swf`，并用 FFDec 与本地反编译文件核对：

- `pinball/common/data/story/ScenarioCommandKind.as`：0..24 枚举。
- `pinball/master/generated/ScenarioCommandValues.as`：字段名、列号、类型及 `(None)` 解析。
- `pinball/scene/storyQuest/scenario/ScenarioLayer.as`：运行时分派。
- `pinball/scene/storyQuest/core/ScenarioCommand.as`：命令推进语义。

当前 1708 文件共有 354232 条指令，实际使用 22 种；16、17、21 当前为 0 次，但客户端定义和执行路径完整。

| kind | enum | 语义 | 参数（0-based） | 当前次数 |
|---:|---|---|---|---:|
| 0 | `Text` | 对白/旁白 | c4 `text_character_id`; c5 `text_body`; c6 `text_voice_path` optional str; c7 `text_voice_immediately` bool; c8 `text_balloon_position` int | 92068 |
| 1 | `Bgm` | 立即播放/切换 BGM | c35 `bgm_channel`; c36 `bgm_id` | 4884 |
| 2 | `BgmStop` | 停止指定/全部 BGM | c37 `bgm_stop_channel` | 12 |
| 3 | `BgmFadeIn` | 淡入播放 BGM | c38 `bgm_fade_in_channel`; c39 `bgm_fade_in_id`; c40 `bgm_fade_in_time` nullable int | 1363 |
| 4 | `BgmFadeOut` | 淡出 BGM | c41 `bgm_fade_out_channel`; c42 `bgm_fade_out_time` nullable int | 4620 |
| 5 | `MovieSequence` | 当前背景 movie 播放 section | c1 `movie_sequence_section`; c2 `movie_sequence_wait`; c3 `movie_sequence_skip_enable` | 71772 |
| 6 | `CharacterIn` | 角色立绘入场 | c12 `character_in_id`; c13 `character_in_position`; c14 `character_in_face` str list; c15 `character_in_reverse` | 38472 |
| 7 | `CharacterOut` | 指定角色退场 | c16 `character_out_id`; c17 `character_out_kind` optional int | 14351 |
| 8 | `CharacterOutAll` | 全部角色退场 | c18 `character_out_all_kind` optional int | 16180 |
| 9 | `CharacterActive` | 高亮指定角色，不强制暗化其他角色 | c9 `character_active_id` | 2557 |
| 10 | `CharacterActiveOnly` | 只高亮指定角色并暗化其他角色 | c10 `character_active_only_id` | 22507 |
| 11 | `CharacterInactive` | 暗化指定角色，不退场 | c11 `character_inactive_id` | 21838 |
| 12 | `CharacterFace` | 更换表情层组合 | c19 `character_face_id`; c20 `character_face_face` str list | 41456 |
| 13 | `CharacterAnimation` | 播放角色动作 | c21 `character_animation_id`; c22 `character_animation_kind` | 19401 |
| 14 | `ScreenShake` | 屏幕震动 | c27 `screen_shake_time` nullable int; c28 `screen_shake_size` nullable int | 396 |
| 15 | `ScreenShakeStop` | 停止屏幕震动 | 无 | 276 |
| 16 | `ScreenFadeIn` | 全屏淡入 | c23 `screen_fade_in_color` nullable int; c24 `screen_fade_in_time` nullable int | 0 |
| 17 | `ScreenFadeOut` | 全屏淡出 | c25 `screen_fade_out_color` nullable int; c26 `screen_fade_out_time` nullable int | 0 |
| 18 | `ScreenColorEffect` | 预设颜色滤镜 | c29 `screen_color_effect_preset`; c30 `screen_color_effect_time` nullable int | 131 |
| 19 | `ScreenColorEffectCustom` | 自定义 4×5 颜色矩阵 | c31 `screen_color_effect_custom_matrix` float list; c32 `screen_color_effect_custom_time` nullable int | 30 |
| 20 | `ScreenColorEffectDisable` | 关闭颜色滤镜 | c33 `screen_color_effect_disable_time` nullable int | 137 |
| 21 | `Background` | 更换静态背景；`(None)` 清除 | c34 `background_path` optional str | 0 |
| 22 | `MessageWindowShow` | 显示消息窗口 | 无 | 1714 |
| 23 | `MessageWindowHide` | 隐藏消息窗口 | 无 | 25 |
| 24 | `NextMovie` | 切到下一份背景 movie | 无 | 42 |

补充语义：BGM channel 0/1 是具体通道，2 是全部；角色入场 position 1/2/3 为左/中/右；角色动作 kind 0..7 为 jump/wave/nod/shake/damage/fall/escape_left/escape_right；`screen_shake_time=0` 表示持续到 kind 15；c31 是 20 个浮点数。

### 与任务书经验表的修正

| kind | 经验判断 | 反编译结论 |
|---:|---|---|
| 4 | 等待 | `BgmFadeOut`；协议没有独立 Wait opcode |
| 5 | 场景切换 | `MovieSequence`，控制当前背景 movie 的 section；静态背景是 kind 21 |
| 7 | 聚焦？ | `CharacterOut` |
| 8 | 全场操作？ | `CharacterOutAll` |
| 9 | 未知 | `CharacterActive` |
| 10 | 说话高亮/聚光 | `CharacterActiveOnly`，会暗化其他角色 |
| 11 | 角色退场 | `CharacterInactive`，只暗化；真正退场是 kind 7 |
| 20 | 未知 | `ScreenColorEffectDisable` |
| 22 | 文件头/初始化 | `MessageWindowShow`；因为常在开头出现而容易误判 |

任务书未观察到但客户端定义的 kind 为 2、14、15、16、17、19、21、23、24。

## 当前验证结果与限制

2026-08-02 当前 store：

- 1708/1708 scenario 解码成功。
- 默认 pathlist 同时强制分类计数 1312/197/157/39/3；空清单、截断、重复或无效剧情行不会得到绿色门禁。
- 354232 条指令，92068 条对白。
- 未知 opcode 0，未解析说话人 0，话名关联缺失 0。
- 角色“白”解析为本体、二周年、圣诞三个 identity-family 成员，共 9 话。
- 白本体三话指令数 157/134/190，对白数 44/44/56；主线 `main_chapter_01_01` 51 条指令，首句为阿尔克“这里就是外面的世界？”。

本期不解析 `movie.movie.amf3.deflate` / timeline，所以 `MovieSequence` 只能显示 section 名，不能解析背景图、镜头和立绘坐标。也不抽取/转码语音，不提供 GUI，不做翻译或改写。
