# 交接 · 2026-08-07 · 塔非破坏性重做 + 公开链补认领 + 图标形状修复

接手提示词见文末「## 交接提示词」，可直接粘给下一个会话。

---

## 〇、一眼看清当前状态

| 项 | 值 |
|---|---|
| 主仓 HEAD | `2a8b7ea4`，分支 `release/modes-20260714`，**领先 0 落后 0**（已推 fork） |
| 工具仓 | `kuronzzhan-droid/startpoint-cn-mod-tools` → `9fe4774`（已推） |
| 本地 CDN 链尾 | **1.4.322** |
| 公开发布链 | 1.4.318（`assets/asset-patch/`，75 个包）+ Release `overlay-1.4.318` |
| CN 服务端 | **在跑**（`192.168.0.130:8001`，`/api/server/currentTime` 返 401 属正常） |
| 工作树 | 已跟踪文件零真实改动（`git status` 可能因 stat 缓存虚报，`git diff --stat` 才是准的） |

**上游 `DontBeAlarmed/startpoint-cn` 全程没动。**

---

## 一、已完成且验证过的

### 1. 深渊连战塔：HP 重排改为非破坏性（本轮最大一件）

**症状**（群反馈 + 作者实测）：刷不到机兵/女帝歼灭者、树妖废墟守卫重复出现好几层、一键重开只有 15 层还要点好久。

**根因：一个 commit，四个症状**——`86e27250`（2026-08-05「深渊连战塔重做」，9693 行）：

- 它给「第 2~30 战全部落在 21,940,625~30,716,875/s 需求带」加了硬约束，实现方式是**血量塞不进带就整层换掉**。实测 30 层塔 **20 层被换**，扫出去的正是策展内容：机兵、女帝歼灭者、土俑嘉年华、五元素球、东亚奇廉CEO，连**终始之龙 / 无幻之宴 / 机工神兵菲诺梅那三个守门固定位**都没保住。替换目标池小 ⇒ 白虎×3、异质魔晶羊×3、伊尔昂斯拉×3、树妖×3。
- 同一 commit 把 `reset_rerolls_tower` 置 null（自述「暂为 null(真机验收期)」），游戏内重置不再触发重摇。
- 同一 commit 新增的 BossKind→专表门禁在 `--mix` 拼接层上**误报**：boss 代号换了但 **BossKind 列没换**，树妖坐进 kraken/orochi/*_sphere 的槽 ⇒ **8 个种子 3 个直接 `[ERR] 解析链断裂`**，reroll 收到非零退出码就「中止(进度未动)」= 作者体感的「要点好久」。

**作者的原始需求（`6d7dec0`）是压低敌方攻击，从来没要求过按血量换 boss。** 已改为非破坏性，连拆五道「不合规就换素材」的闸，详见 commit `49ccf2fb`。

验收（10 seed × 30 层 hell+mix）：构建成功率 3/8 失败 → **10/10**；整层替换 20 层 → 1~4 层；拼接层 4 个不同 boss → **8 个**；攻击带完好（中后段 col 中位 1.10 / P90 1.33 / max 1.35）。

### 2. 系列配额再挂变体键

1.4.316 真机实测第 12/14 战都是暗荒龙。`系列:荒龙` 配额 2 的本意是「同塔来两只**不同**元素变体」，但 `discarded_dragon_dark_tower` 与 `discarded_dragon_dark` 是两个代号同一模型，各吃一个名额。已给系列成员额外挂变体键（配额 1）；**只给系列成员挂**，否则会把八岐大蛇各头压成一个。commit `bc65bbb8`。

### 3. 重开链路恢复

- `assets/rogue_event.json` 的 `reset_rerolls_tower` 恢复，但**发布的值保持 null**，改由环境变量 `WF_ROGUE_REROLL_ON_RESET` 开（作者 `.env` 已配好）。理由见 §四·2。
- 「只能 15 层」= **三个入口各自写死**：CLI default、`wf_gui.html` 轮数框是 **placeholder 不是 value**（留空被前端丢弃）、`/rogue/auto/run` 的 handler **完全不接请求体**。全部改 30。

### 4. 公开发布链补齐三处认领缺行 → 1.4.318

冻结合同的认领清单漏行，导致三张共享表按契约**回落基线**、收方拿到的不是本服内容：

| 表 | 症状 |
|---|---|
| `character.orderedmap` | 261089 暗龙稀有度停在 ★4（本服 ★5）。`light_dark_dragons` 认领了 6 张表却漏了这张，星级在它**第 2 列** |
| `character_speech.orderedmap` | 杰拉德 149999 的 21 条日配语音缺失 |
| `ability_soul.orderedmap` | 451 键里 408 键停在旧值 |

**修法不是重发 1.4.312**：`cn-asset-graph.ts:126` 的 `reachablePaths` 只走 `from == 当前版本` 的边，已在 1.4.312 的客户端**没有出边**，同版本号重发投递为零；改现有边的终点也没用（起点不是 1.4.312）。**唯一有效是新增一条从 1.4.312 出发的边** → `local-live-fixups` 1.4.312→1.4.318，单包 453 KB。现有 74 包/257 条合同/回执一字未动。

回执门禁同时改成 append-only（判据从「在末尾」改成「在当初写入的绝对位置」，`output_sha256` 比「发布当时的 manifest」）。改之前用真实数据自证：重建 `patches[:15]` 的 sha256 == 回执记录的 `25d8ecad…`。

overlay 也重发了（旧包内层携带的就是 ★4 字节）：新建 Release `overlay-1.4.318`，旧 `overlay-1.4.312` **没删**（已有下载）、标为「已被取代」。

### 5. 发布闸门补内容体检

键闸门只比键集合，比不出「键一个没少、整表被换成另一个来源」（1.4.307 就是这么把 409 个官方魂珠键回退掉的）。新增 `content_notes` 两条判据（改动面 >20 键且 >25%；键内记录条数变少），**只打印永不阻断**。用全部 30 条历史 `ability_soul` 边验过信噪比：只有 6 条被标，1.4.164→1.4.301 那次正常的 15 把深渊武器零告警。

上线当天露了两个洞，已修：告警前缀不能用「⚠」（发布预检子进程 stdout 是 cp936，U+26A0 直接中止发布）；nested 表不能数 `\n` 当记录条数（`rush_event_quest[700099]` 被误报「57→47 条」）。

### 6. 角色图标形状蒙版 → 1.4.320

**作者报「技能指引和技能连锁都超出边框」。** 从 60 个官方角色反推出标准蒙版：**连携=六边形 / 技能准备=竖圆角水滴形 / 状态=圆 / square_round=圆角方**（与既有记忆一致）。套 `min(原图, 蒙版)`，纯减法。

最严重：基诺维 `battle_control_board` 越界 **99%**（实心矩形）、`cutin_skill_chain` 66%；史黛拉连携 58%（纯矩形）。杰拉德本来就基本干净（5~6%）——所以作者说「杰拉德好像修好了」是对的。

同批还修了技能图左偏（官方规范是内容贴左边、右留 18.5%）与杰拉德头像重切（全身照 → 头肩特写，主体占框 53.9% → 74.7%）。共 **60 个 PNG**，全在 medium 根，越界复检 **0 项**。

---

## 二、⚠ 未解决 / 我搞错过的

### 1. 队伍卡片「脸太小、感觉好远」——**无解，别再用 faceY 折腾**

**我在这上面翻过车，回滚了（1.4.321 → 1.4.322 撤回）。**

公式（`FullShotImageAttributeTools.as:getOffsetToCenterizeFace`，已确证）：

```
offset = scale × (pivotY − faceY)
```

**方向是：faceY 调小 → offset 变大 → 立绘往下推。** 我一开始反过来理解了，把杰拉德 551→250，结果角色详情页的立绘下移 301px（作者截图实锤）。已全部还原并发布 1.4.322，与改动前语义逐键一致。

**真正根因**：官方美术是**大头身**，我们的 AI 立绘是**七八头身**。同样锚在胸口，我们的头就是小。faceY 只能改「露出哪一段身体」，改不了「头有多大」。而且**一个值两个界面共用**（队伍卡片 + 角色详情页），诉求矛盾。

要解决只能**重裁 `full_shot` 本身**做成更近的构图——改动面大且会改变角色详情页观感，**建议单开一轮做，别顺手动**。

### 2. 已核实但仍待办

- **暗龙 261089 的 faceY = 0.446**（官方四分位 0.254~0.306），作者明确说「暗龙不用」，未动。
- **风巨蜥槽1 faceX=1262 / 光龙槽1 faceX=693** 都在官方 972~1039 之外，未动。
- **`ability_soul` 1.4.307 的 409 键回退**：作者决定**不回滚**（「其他角色和武器好不容易没问题了」），且那是既成事实、他验的就是变更后的状态。**别再提回退。**
- **塔的第 4 战召唤物不出现**（上一轮交接的主线）：本轮没碰。上一轮已排除 funnel 缺失/DSL 缺失/zako 差异三条，建议的下一步是把 boss4 的 c111 临时还原成官方单条 shot 做判别实验。
- 备份分支 `backup/pre-rebase-20260807`、`backup/pre-rebase2-20260807` 可删。

---

## 三、本轮发布的边（本地 CDN）

| 版本 | 内容 | 大小 |
|---|---|---|
| 1.4.316 | 非破坏性改造后的第一座塔 | 646 KB |
| 1.4.317 | 延续的黄金 `equipment_status[100012]` → 99999/99999 | 9.6 KB |
| 1.4.318 | **桥接边** 1.4.317→1.4.318（见 §四·1） | 643 KB |
| 1.4.319 | 换的新塔（seed **20518212**，零元素变体重复） | 643 KB |
| 1.4.320 | 60 个图标 PNG（形状蒙版 + 技能图 + 杰拉德头像） | 5.4 MB(medium) |
| 1.4.321 | 立绘定位表改动 —— **已被 1.4.322 撤回** | 13 KB |
| 1.4.322 | 撤回 1.4.321 | 13 KB |

**全版本可达性已验**（1.4.0/54/277/312/316/317/318/319/320/321 → 1.4.322 全通）。

---

## 四、踩过的坑（省时间用）

### 1. `wf_publish` 会跳过本地链尾，造成死路

`current_max_version()` **故意把 asset-patch 的版本纳入 max**。我发了公开链 1.4.318 之后，本地链尾其实是 1.4.317，于是重摇发出来的边是 `1.4.318→1.4.319`，**整条 1.4.313~1.4.317 被孤立**——包括作者自己那台（当时在 1.4.317）。已补桥 `1.4.317→1.4.318`。

**铁律：每次发布后跑一次全版本可达性检查**：

```bash
node -e "const g=require('./out/lib/cn-asset-graph.js');const s=g.getCnReleaseGraphSnapshot();
for(const v of ['1.4.0','1.4.312','1.4.319','1.4.322']){const r=g.findReleasePath(s,v);
console.log(v+' -> '+r.targetVersion+' ('+r.edges.length+'边)');}"
```

### 2. 发出去的服务端资产 ≠ 作者本机配置

`assets/rogue_event.json` 是**跟着仓库发给别人的**。`reset_rerolls_tower` 一旦非空，别人自建的服上有人按「战斗记录&重置」，就会在**他们的机器**上 spawn `wf_rogue_reroll.py --apply`：重建塔、写他们的 `.cdn` 推版本号、清那台服所有存档的进度。已改成环境变量开关。
**判据：凡是会在服务端机器上产生副作用的开关，默认值必须对陌生部署安全。**

### 3. 服务端 json 的 CRLF 会让发布回执静默变红

`npm run verify:local-release` 报 `server terminal evidence mismatch` 而 `git diff` 一行差异都没有 —— Windows 上 Python 文本模式默认写 CRLF，git 的 `text=auto` 把差异归一化掉（status 干净），但回执 `_server_evidence` 哈希的是**原始字节**。实测 7 个服务端 json 全中招。已给三个写入点补 `newline="\n"`（`wf_rogue_build.py` 两处 + `wf_gui.py` 一处）。**新增写服务端 json 的代码务必带上。**

### 4. 排查工具的三个陷阱

- **`git status` 会因 stat 缓存虚报改动**，`git diff --stat` 才是准的；真要定死就 `git show HEAD:<path>` 取 blob 和磁盘比字节。
- **本 CDN 的 PNG 魔数是小写 `\x89png`**，PIL 认不出，读前要补成大写、写回要还原成小写。
- **medium/android 根的资源 `--tables` 发不了**（它只按 common 解析）。要走 `sync_pending.json` 写 `medium:<2位>/<38位>` 前缀，然后 `wf_publish` 不带 `--tables`。发完 pending 会被自动清空。

### 5. 本仓的序列化器复现不出原始压缩流

改 `full_shot_image_attribute` 时，**语义只变 3 个键、字节却变了 505 个**（41405→43692 B）。试遍 zlib 全部 10 个级别都还原不了原字节。这与当初回执校验器从「重新 deflate 比字节」改成结构性检查是同一个根因。**改嵌套表前先跑 `q.build_node(tree)==原字节`，False 就要预期到内容告警会响。**

### 6. 其它

- `wf_quest_lib.save_table` 自带 build→parse 自校验 + 自动备份，改表优先用它。嵌套表内层值是 **str 不是 bytes**。
- 导出仓同步走**三分法**，`diff` 必带 `--strip-trailing-cr`，**同步前必先 fetch**（本轮本地克隆又落后 4 提交）。
- Bash 工具 cwd 在调用间保留，一律用绝对路径。

---

## 交接提示词

> 继续 StarPoint CN。先读 `D:\WF\startpoint-cn\AGENTS.md`、wf-mod skill、以及
> `mod-tools/docs/交接-20260807-塔非破坏性重做与图标形状修复.md`（全文，尤其
> §二「未解决 / 我搞错过的」与 §四「踩过的坑」——那些都已证伪或已证实，别重走）。
>
> **当前状态**：主仓 HEAD `2a8b7ea4`（领先 0 落后 0，已推 fork）；工具仓 `9fe4774`；
> 本地 CDN 链尾 **1.4.322**；公开链 1.4.318 + Release `overlay-1.4.318`；
> CN 服务端在跑（`192.168.0.130:8001`，401 属正常）。
>
> **可以接着做的**（按价值排序，都不紧急）：
> 1. **塔第 4 战召唤物不出现** —— 上一轮交接的主线，本轮没碰。已排除 funnel 缺失 /
>    DSL 缺失 / zako 差异；建议第一步是把 `mod_rogue_boss4` 的 c111 临时还原成官方
>    单条 shot 发一版，判定是不是追加的跨家族 shot 干扰了召唤。
> 2. **队伍卡片脸太小** —— ⚠ **不要碰 faceY**（我试过，方向搞反、已回滚，见 §二·1）。
>    真要解决只能重裁 `full_shot` 立绘做成更近的构图，单开一轮。
> 3. 暗龙 faceY 0.446 偏离官方区间（作者说「暗龙不用」，要动先问）；
>    风巨蜥槽1 / 光龙槽1 的 faceX 也在官方区间外。
>
> **纪律**：
> - 每次发布后**必跑全版本可达性检查**（§四·1 给了命令）——本轮因为漏了这步，
>   把作者自己那台客户端孤立在 1.4.317 死路上。
> - 发布前逐键比一次「公开包 vs 本地 store」，键集合检查抓不到内容被整表替换。
> - 会在服务端机器上产生副作用的开关，发布默认值必须对陌生部署安全。
> - **ability_soul 1.4.307 的 409 键回退：作者已决定不动，别再提。**
> - 改美术资源前先量官方基线（本轮所有图标结论都是从 40~60 个官方角色统计出来的），
>   别凭观感改；改完必须复检越界率/占框比。
