# 任务系统后续路线图

本文是任务模块当前阶段完成后的长期计划，描述下一步补全顺序和验收边界。它不是客户端测试记录；实际通过状态以[测试进度](../status/test-progress.md)为准。

## 当前基线

基线由 `src/lib/mission/coverage-audit.ts` 和 `tools/mission_coverage_audit.test.cjs` 生成；文档数字不得脱离机器分区单独更新。

| 模块 | 当前服务端状态 | 客户端状态 |
|---|---|---|
| 角色觉醒 | 144 条按真实条件族唯一分区并进入权威计算路径；55 条通用白名单 | 核心解锁和第一页领奖已通过；种族合集与无阵亡规则已闭合，全部条件仍待逐条验收 |
| 每日任务 | 默认服务器时间下当前开放 `11/11`；常驻 all-clear 不包含活动任务 | 每日提示、发奖、跨日和重新 load 待完整验收 |
| 每周任务 | 官方当前定义 `2/2`；周一北京时间 05:00 边界和周期基线已有自动测试 | 跨周真实客户端流程待验收 |
| 称号 | 自动计算 `1281/1288`，约 99.5%；其余 7 条已按精确 ID 归档为权威事实阻塞，并保留持久化 fallback | 已实现条件族的提示、领奖和持久化待验收 |
| Active Mission | 96 条定义中 40 个 pattern、86 条定义已有事实链；20011～20017 已完成 | 客户端验收由贡献者异步进行，不阻塞后续开发 |
| 活动任务 | category 3 已启用 `2485/2512` 条事实：805 条原生严格协力规则、948 条已审计空 selector 兼容规则、257 条 type 23 精确通关规则、445 条关卡/物品/竞速/阶段/当前状态/单人累计规则、18 条 Event 入口/SET/投票事实、2 条歼灭者 type 86 及 10 条 type 87；剩余 27 条救援保持持久化 fallback | 已实现规则仍需分批客户端验收 |
| Pass | category 6/7/8 共 `248/267` 条已有权威事实，点数和等级奖励已实现 | 救援 19 条和购买流程保持独立缺口 |

## 下一阶段顺序

### 1. 补全 Active Mission 的单人可证明事实

优先处理：

1. `20017` 已按 CN 1.8.1 的主位首次弹射统计实现；
2. `20015/20016` 已从官方 CDN 建立技能动作效果索引，并匹配属性耐性下降、直接回复、比例回复和再生效果；
3. 称号条件族已完成本阶段收尾；后续只在新增权威事实生产者后重新开放对应延期 ID。

缺少动作效果索引或无法确认统计语义时保持 fail closed，不按角色名称或中文文案推测。20015/20016 的
未解析角色、队伍快照边界和任务开放前是否预累计见[待审阅实现记录](mission-implementation-assumptions.md)。

### 2. 称号阶段收尾

称号保持 `1281/1288` 自动计算。剩余 7 条不是继续按文案尝试实现的普通待办，而是以下精确延期集合：

| ID | 数量 | 延期原因 | 重新开放前置 |
|---|---:|---|---|
| `3000` | 1 | CN CDN 的 `character_level` 与客户端按 1～5 星索引的结构不能闭合，现有四、五星曲线也没有 Lv60 阈值 | 取得可复核的完整五档官方等级曲线 |
| `25000/25010/25020` | 3 | 普通 Guest 结算不能证明 Attention 救援来源 | 联机入口持久化可验证的 Attention 来源 |
| `70004/70005/70006` | 3 | 当前房间与 finish 数据不能权威区分“新手组队战斗” | 协议或房间状态提供官方新手分类 |

`pattern=48` 第二玛纳板条件族已完成：3 条全局节点累计和 472 条指定角色全板完成。本阶段不再扩展称号；上表任一前置闭合后，才按同一安全下界原则单独恢复对应 ID。

### 3. 分批补全活动任务与 Pass

category 3 的 QuestRange、评级、房主/成员、阶段和 HardMulti 战斗条件已按类型化事实拆分，禁止重新启用旧宽松 map。剩余 Attention 救援来源不明确时不冒充普通 Guest。Pass 优先处理有明确单人事实的 pattern，购买流程不与免费轨混合。

战阵 type 80/81/82 的 12 条任务仅以 SET 编辑器成功保存 RAID 第 1 组主队伍、副 1、副 2 槽位作为权威事实。服务端请求无法证明用户是否点击过客户端“复制”按钮，因此不得把这批实现描述为复制按钮追踪。

## 明确延后

- category 3 剩余 27 条 Attention 救援任务：本轮不实现；
- 每日/每周任务的客户端提示、发奖、跨日/跨周和重启持久化完整验收：本轮不继续；
- 角色觉醒 144 条条件全集逐条对齐与客户端验收：核心时序保持现状，本轮不继续；
- 接取救援请求和 Attention：缺少权威救援来源，保持低优先级、fail closed；
- 回归任务：资格生产者完成前不向普通玩家开放；
- Active Mission `21030`：依赖尚未实现的 `multi_special_exchange` 活动入口；
- 完整数据库导入导出：本路线图不扩展所有任务事实的迁移格式；
- 上表 7 条称号：只在对应权威前置出现后重新开放，不使用近似值补齐。MVP 已按官方客户端 `statistics.is_mvp` 接入，但不声称服务端独立重算。

## 每批完成门槛

每个条件族必须独立提交，并通过：

```bash
node tools/test-workflow/run.cjs --group integration:mission
node tools/docs_check.cjs
npm run build
bash scripts/check-hygiene.sh
git diff --check
```

提交后不自动 push。客户端验收由贡献者异步进行，不作为继续开发的前置条件；规则实现、架构文档和验收结果必须分别保持中文且不互相冒充。
